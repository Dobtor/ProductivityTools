<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_xmind_import_wizard_form" model="ir.ui.view">
        <field name="name">xmind.import.wizard.form</field>
        <field name="model">xmind.import.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <p class="text-muted">
                        Select one or more .xmind, .mm (FreeMind), or .mmap (MindManager) files.
                        Each file will be created as a separate Mind Map workbook with a preview thumbnail.
                    </p>
                </group>
                <group>
                    <field name="upload_files" widget="many2many_binary"
                           string="Upload Files"/>
                </group>
                <field name="line_ids" readonly="1" invisible="not line_ids">
                    <list>
                        <field name="filename"/>
                        <field name="status"
                               decoration-success="status == 'success'"
                               decoration-danger="status == 'error'"
                               widget="badge"/>
                        <field name="message"/>
                        <field name="workbook_id"/>
                    </list>
                </field>
                <group invisible="not line_ids">
                    <div class="d-flex gap-2">
                        <field name="import_count" string="Imported" widget="statinfo"/>
                        <field name="error_count" string="Errors" widget="statinfo"/>
                    </div>
                </group>
                <footer>
                    <button name="action_import_all" string="Import" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_xmind_import_wizard" model="ir.actions.act_window">
        <field name="name">Import Mind Maps</field>
        <field name="res_model">xmind.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
