<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_note_transcribe_job_list" model="ir.ui.view">
        <field name="name">note.transcribe.job.list</field>
        <field name="model">note.transcribe.job</field>
        <field name="arch" type="xml">
            <list default_order="priority desc, scheduled_at, id"
                  decoration-info="state == 'pending'"
                  decoration-warning="state == 'running'"
                  decoration-success="state == 'done'"
                  decoration-danger="state == 'failed'"
                  decoration-muted="state == 'cancelled'">
                <field name="create_date" string="Created"/>
                <field name="recording_id"/>
                <field name="note_id"/>
                <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                <field name="priority" optional="hide"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'pending'"
                       decoration-warning="state == 'running'"
                       decoration-success="state == 'done'"
                       decoration-danger="state == 'failed'"/>
                <field name="scheduled_at" optional="hide"/>
                <field name="picked_at" optional="show"/>
                <field name="ended_at" optional="show"/>
                <field name="worker_pid" optional="hide"/>
                <field name="error_message" optional="show"/>
                <button name="action_cancel" type="object"
                        string="Cancel" icon="fa-times" class="btn-link"
                        invisible="state not in ('pending','running')"/>
                <button name="action_requeue" type="object"
                        string="Requeue" icon="fa-refresh" class="btn-link"
                        invisible="state not in ('failed','cancelled')"/>
            </list>
        </field>
    </record>

    <record id="view_note_transcribe_job_form" model="ir.ui.view">
        <field name="name">note.transcribe.job.form</field>
        <field name="model">note.transcribe.job</field>
        <field name="arch" type="xml">
            <form create="false">
                <header>
                    <button name="action_cancel" type="object"
                            string="Cancel" class="btn-warning"
                            invisible="state not in ('pending','running')"/>
                    <button name="action_requeue" type="object"
                            string="Requeue" class="btn-primary"
                            invisible="state not in ('failed','cancelled')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="pending,running,done"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="recording_id" readonly="1"/>
                            <field name="note_id" readonly="1"/>
                            <field name="user_id" readonly="1"/>
                            <field name="priority"/>
                            <field name="log_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="scheduled_at"/>
                            <field name="picked_at" readonly="1"/>
                            <field name="started_at" readonly="1"/>
                            <field name="ended_at" readonly="1"/>
                            <field name="worker_pid" readonly="1"/>
                        </group>
                    </group>
                    <group string="Error Message" invisible="not error_message">
                        <field name="error_message" nolabel="1" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_note_transcribe_job_search" model="ir.ui.view">
        <field name="name">note.transcribe.job.search</field>
        <field name="model">note.transcribe.job</field>
        <field name="arch" type="xml">
            <search>
                <field name="recording_id"/>
                <field name="note_id"/>
                <filter name="filter_active" string="Active (pending/running)"
                        domain="[('state','in',('pending','running'))]"/>
                <filter name="filter_failed" string="Failed" domain="[('state','=','failed')]"/>
                <filter name="filter_done" string="Done" domain="[('state','=','done')]"/>
                <separator/>
                <filter name="filter_today" string="Today"
                        domain="[('create_date','&gt;=',context_today().strftime('%Y-%m-%d 00:00:00'))]"/>
                <group expand="0" string="Group By">
                    <filter name="group_state" string="State" context="{'group_by': 'state'}"/>
                    <filter name="group_user" string="User" context="{'group_by': 'user_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_note_transcribe_job" model="ir.actions.act_window">
        <field name="name">Transcription Jobs</field>
        <field name="res_model">note.transcribe.job</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_note_transcribe_job_search"/>
        <field name="context">{'search_default_filter_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No jobs yet</p>
            <p>Every transcription request creates a job here — survives Odoo restart.</p>
        </field>
    </record>

</odoo>
