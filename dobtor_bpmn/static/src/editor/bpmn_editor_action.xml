<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="dobtor_bpmn.BpmnEditorAction">
        <div class="o_bpmn_action d-flex flex-column">
            <div class="o_bpmn_action_toolbar d-flex align-items-center p-2 gap-2">
                <button class="btn btn-secondary btn-sm" t-on-click="onBack">
                    <i class="fa fa-arrow-left me-1"/>返回清單
                </button>
                <span class="fw-bold mx-2" t-esc="state.name"/>
                <span class="badge text-bg-secondary text-uppercase" t-esc="state.diagramType"/>
                <span t-if="state.dirty" class="badge text-bg-warning">未儲存</span>
                <div class="flex-grow-1"/>
                <button class="btn btn-primary btn-sm" t-on-click="onSave"
                        t-att-disabled="state.saving or state.libMissing">
                    <i class="fa fa-save me-1"/>
                    <t t-if="state.saving">儲存中…</t>
                    <t t-else="">儲存</t>
                </button>
            </div>

            <div t-if="state.error" class="alert alert-danger m-2" role="alert">
                <span t-esc="state.error"/>
            </div>

            <div t-if="state.libMissing" class="alert alert-warning m-2" role="alert">
                <h5>無法載入 bpmn.io 視覺編輯器函式庫</h5>
                <p>
                    已嘗試本地（<code>static/lib/bpmn-io/</code>）與 CDN（unpkg）皆失敗。
                    常見原因：無法連外 / CSP 擋外部腳本 / 本地未放置 dist 檔。
                    請參考 <code>dobtor_bpmn/static/lib/bpmn-io/README.md</code>。
                </p>
            </div>

            <div t-if="!state.libMissing and !state.error" class="o_bpmn_action_body d-flex flex-grow-1">
                <div class="o_bpmn_action_canvas flex-grow-1" t-ref="canvas"/>
                <div class="o_bpmn_action_panel" t-ref="panel"/>
            </div>
        </div>
    </t>
</templates>
