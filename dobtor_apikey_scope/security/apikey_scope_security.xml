<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Regular internal users may only see/edit the permission scopes of
             their own API keys. -->
        <record id="apikey_scope_own_rule" model="ir.rule">
            <field name="name">API Key Scope: own keys only</field>
            <field name="model_id" ref="model_res_users_apikeys_scope"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Settings administrators may manage every key's scope. Group rules
             are OR-combined, so this widens group_system users to all rows. -->
        <record id="apikey_scope_admin_rule" model="ir.rule">
            <field name="name">API Key Scope: administrators see all</field>
            <field name="model_id" ref="model_res_users_apikeys_scope"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_system'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>
